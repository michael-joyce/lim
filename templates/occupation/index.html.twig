{% extends 'base.html.twig' %}

{% block title %}Occupation List{% endblock %}

{% block pageheader %}
    <h1>Occupation List</h1>
    <p class="count">
        Displaying {{ occupations|length }} occupations of {{ occupations.getTotalItemCount }} total.
    </p>
{% endblock %}

{% block body %}

    <div class='btn-toolbar pull-right'>
        <div class='btn-group'>
            {% if is_granted('ROLE_CONTENT_ADMIN') %}
                <a href="{{ path('occupation_new') }}" class="btn btn-default">
                    <span class="glyphicon glyphicon-plus"></span> New </a>
            {% endif %}

            <a href="{{ path('occupation_search') }}" class="btn btn-default">
                <span class="glyphicon glyphicon-search"></span> Search </a>
        </div>
    </div>

    {% embed 'occupation/partial/table.html.twig' %}
    {% endembed %}

    <div class="navigation">
        {{ knp_pagination_render(occupations) }}
    </div>

{% endblock %}
