{{ form_start(form) }}
{{ form_widget(form) }}
<div class="form-group">
    <div class="col-sm-2"></div>
    <div class="col-sm-10">
        <input type="submit" value="Save" class="btn btn-primary" />
        {% if reference_role.id is null %}
        <a href="{{ path('reference_role_index') }}" class="btn btn-warning">
            Cancel
        </a>
        {% else %}
            <a href="{{ path('reference_role_show', { 'id': reference_role.id }) }}" class="btn btn-warning">
                Cancel
            </a>
        {% endif %}
    </div>
</div>
{{ form_end(form) }}

